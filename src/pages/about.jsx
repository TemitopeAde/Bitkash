import React from 'react'
import clx from '../utils/clx'
import AboutWrapper from '../views/about-wrapper'

export default function About({
      classname,
      children
}){
      return (
            <AboutWrapper />
      )
}